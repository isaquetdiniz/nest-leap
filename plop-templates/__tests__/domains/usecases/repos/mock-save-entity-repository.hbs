import { {{pascalCase name}} } from '@/domains/{{dashCase name}}/entities';
import { ISave{{pascalCase name}}Repository } from '@/domains/{{dashCase name}}/usecases/repos';
import { mock{{pascalCase name}}Entity } from '@/tests/domains/{{dashCase name}}/entities/mocks/{{dashCase name}}-mocks';

export const mockCreate{{pascalCase name}}RepositoryParams =
  (): ISave{{pascalCase name}}Repository.Params => mock{{pascalCase name}}Entity();

export class Save{{pascalCase name}}RepositorySpy implements ISave{{pascalCase name}}Repository {
  {{camelCase name}} = new {{pascalCase name}}(mock{{pascalCase name}}Entity());

  save({{camelCase name}}Params: {{pascalCase name}}): Promise<{{pascalCase name}}> {
    return Promise.resolve(this.{{camelCase name}});
  }
}
