import { IGet{{pascalCase name}}ByIdRepository } from '@/domains/{{dashCase name}}/usecases/repos';
import { mock{{pascalCase name}}Entity } from '@/tests/domains/{{dashCase name}}/entities/mocks/{{dashCase name}}-mocks';

export class Get{{pascalCase name}}ByIdRepositorySpy implements IGet{{pascalCase name}}ByIdRepository {
  result = mock{{pascalCase name}}Entity();

  getById(id: string): Promise<IGet{{pascalCase name}}ByIdRepository.Result> {
    if (id === '') return Promise.resolve(null);
    return Promise.resolve(this.result);
  }
}
