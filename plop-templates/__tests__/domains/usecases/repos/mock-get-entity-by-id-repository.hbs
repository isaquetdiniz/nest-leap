import { IGet{{pascalCase name}}ByIdRepository } from '@/domains/{{dashCase name}}';
import { mockCreate{{pascalCase name}}Result } from '@/tests/domains/{{dashCase name}}/entities/mocks/{{dashCase name}}-mocks';

export class Get{{pascalCase name}}ByIdRepositorySpy implements IGet{{pascalCase name}}ByIdRepository {
  result = mockCreate{{pascalCase name}}Result();

  get(id: string): Promise<IGet{{pascalCase name}}ByIdRepository.Result> {
    if (id === '') return Promise.resolve(null);
    return Promise.resolve(this.result);
  }
}
