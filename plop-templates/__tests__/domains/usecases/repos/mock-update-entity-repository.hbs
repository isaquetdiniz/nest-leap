import { {{pascalCase name}}, IUpdate{{pascalCase name}}Repository } from '@/domains/{{dashCase name}}/usecases/repos';

import { mock{{pascalCase name}}Entity } from '@/tests/domains/{{dashCase name}}/entities/mocks/{{dashCase name}}-mocks';

export const mockUpdate{{pascalCase name}}RepositoryParams =
  (): IUpdate{{pascalCase name}}Repository.Params => mock{{pascalCase name}}Entity();

export class Update{{pascalCase name}}RepositorySpy implements IUpdate{{pascalCase name}}Repository {
  {{camelCase name}} = new {{pascalCase name}}(mock{{pascalCase name}}Entity());

  update({{camelCase name}}ToUpdate: {{pascalCase name}}): Promise<{{pascalCase name}}> {
    return Promise.resolve(this.{{camelCase name}});
  }
}
