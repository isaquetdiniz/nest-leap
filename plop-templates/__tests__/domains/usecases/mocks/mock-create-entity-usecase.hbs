import { {{pascalCase name}} } from '@/domains/{{dashCase name}}/entities';
import { ICreate{{pascalCase name}}Usecase } from '@/domains/{{dashCase name}}/usecase';
import { mock{{pascalCase name}}Entity } from '@/tests/domains/{{dashCase name}}/entities/mocks/{{dashCase name}}-mocks';

export class Create{{pascalCase name}}UsecaseSpy implements ICreate{{pascalCase name}}Usecase {
  result = mock{{pascalCase name}}Entity();

  execute(params: ICreate{{pascalCase name}}Usecase.Params): Promise<{{pascalCase name}}> {
    return Promise.resolve(this.result);
  }
}
