import { {{pascalCase name}}Filters, IGet{{pascalCase name}}sByFilterUsecase } from '@/domains/{{dashCase name}}/usecase';
import { mock{{pascalCase name}}Entity } from '../../entities/mocks/{{dashCase name}}-mocks';

export class Get{{pascalCase name}}sByFilterUsecaseSpy implements IGet{{pascalCase name}}sByFilterUsecase {
  result: IGet{{pascalCase name}}sByFilterUsecase.Result = {
    {{camelCase name}}s: [mock{{pascalCase name}}Entity()],
    total{{pascalCase name}}s: 1,
  };

  execute(listParams: {{pascalCase name}}Filters): Promise<IGet{{pascalCase name}}sByFilterUsecase.Result> {
    return Promise.resolve(this.result);
  }
}
