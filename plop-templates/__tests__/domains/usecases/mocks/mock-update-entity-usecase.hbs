import { {{pascalCase name}} } from '@/domains/{{dashCase name}}/entities';
import { IUpdate{{pascalCase name}}ByIdUsecase } from '@/domains/{{dashCase name}}/usecase';
import { mock{{pascalCase name}}Entity } from '../../entities/mocks/{{dashCase name}}-mocks';

export class Update{{pascalCase name}}ByIdUsecaseSpy implements IUpdate{{pascalCase name}}ByIdUsecase {
  result = mock{{pascalCase name}}Entity();

  execute(updateParams: IUpdate{{pascalCase name}}ByIdUsecase.Params): Promise<{{pascalCase name}}> {
    return Promise.resolve(this.result);
  }
}
