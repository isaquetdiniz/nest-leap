import { IGet{{pascalCase name}}byIdUsecase } from '@/domains/{{dashCase name}}/usecase';
import { mock{{pascalCase name}}Entity } from '../../entities/mocks/{{dashCase name}}-mocks';

export class Get{{pascalCase name}}ByIdUsecaseSpy implements IGet{{pascalCase name}}byIdUsecase {
  result: IGet{{pascalCase name}}byIdUsecase.Result = mock{{pascalCase name}}Entity();

  execute(id: string): Promise<IGet{{pascalCase name}}byIdUsecase.Result> {
    return Promise.resolve(this.result);
  }
}
