import { Controller } from '@/presentation/http/protocols';
import { List{{name}}sController } from '@/presentation/http/controllers/{{dashCase name}}';

import { makeList{{name}}sInDatabaseUsecase } from '@/main/factories/services/{{dashCase name}}';
import { makeLogControllerDecorator } from '@/main/factories/controllers';
import { makeList{{name}}sValidation } from '@/main/factories/validation/{{dashCase name}}';

export const makeList{{name}}sController = (): Controller => {
  const list{{name}}sUsecase = makeList{{name}}sInDatabaseUsecase();

  const validation = makeList{{name}}sValidation();

  const list{{name}}sController = new List{{name}}sController(
    validation,
    list{{name}}sUsecase
  );

  const list{{name}}sControllerWithLogDecorator =
    makeLogControllerDecorator(list{{name}}sController);

  return list{{name}}sControllerWithLogDecorator;
};
