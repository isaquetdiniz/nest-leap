import { Controller } from '@/application/http-server/protocols';
import { Delete{{name}}Controller } from '@/application/http-server/controllers/{{dashCase name}}';

import { makeDelete{{name}}InDatabaseUsecase } from '@/main/factories/services/{{dashCase name}}';
import { makeDelete{{name}}Validation } from '@/main/factories/validation/{{dashCase name}}';
import { makeLogControllerDecorator } from '@/main/factories/controllers';

export const makeDelete{{name}}Controller = (): Controller => {
  const delete{{name}}Usecase = makeDelete{{name}}InDatabaseUsecase();

  const validation = makeDelete{{name}}Validation();

  const delete{{name}}Controller = new Delete{{name}}Controller(
    validation,
    delete{{name}}Usecase
  );

  const delete{{name}}ControllerWithLogDecorator =
    makeLogControllerDecorator(delete{{name}}Controller);

  return delete{{name}}ControllerWithLogDecorator;
};
