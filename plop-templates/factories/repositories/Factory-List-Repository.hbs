import { List{{name}}sInDatabaseRepository } from '@/application/protocols/database/repositories/{{lowerCase name}}';
import { PrismaList{{name}}sInDatabaseRepository } from '@/infra/database/orm/prisma/repositories/{{lowerCase name}}';

export const makePrismaList{{name}}sInDatabaseRepository =
  (): List{{name}}sInDatabaseRepository => {
    return new PrismaList{{name}}sInDatabaseRepository();
  };
