import { PrismaCreate{{name}}InDatabaseRepository } from '@/infra/database/orm/prisma/repositories/{{dashCase name}}';

export const makePrismaCreate{{name}}InDatabaseRepository =
  (): PrismaCreate{{name}}InDatabaseRepository => {
    return new PrismaCreate{{name}}InDatabaseRepository();
  };
