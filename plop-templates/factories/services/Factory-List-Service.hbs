import { List{{name}}sInDatabaseService } from '@/application/services/{{lowerCase name}}';
import { List{{name}}sUsecase } from '@/domain/usecases/{{lowerCase name}}';
import { makePrismaList{{name}}sInDatabaseRepository } from '@/main/factories/infra/database/orm/prisma/repositories/{{lowerCase name}}';

export const makeList{{name}}sInDatabaseUsecase = (): List{{name}}sUsecase => {
  const list{{name}}sInDatabaseRepository =
    makePrismaList{{name}}sInDatabaseRepository();

  return new List{{name}}sInDatabaseService({
    list{{name}}sInDatabaseRepository,
  });
};
