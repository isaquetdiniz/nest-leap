import { List{{name}}sInDatabaseService } from '@/application/services/{{dashCase name}}';
import { List{{name}}sUsecase } from '@/domain/usecases/{{dashCase name}}';
import { makePrismaList{{name}}sInDatabaseRepository } from '@/main/factories/infra/database/orm/prisma/repositories/{{dashCase name}}';

export const makeList{{name}}sInDatabaseUsecase = (): List{{name}}sUsecase => {
  const list{{name}}sInDatabaseRepository =
    makePrismaList{{name}}sInDatabaseRepository();

  return new List{{name}}sInDatabaseService({
    list{{name}}sInDatabaseRepository,
  });
};
