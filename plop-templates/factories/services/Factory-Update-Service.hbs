import { Update{{name}}InDatabaseService } from '@/application/services/{{dashCase name}}';
import { Update{{name}}Usecase } from '@/domain/usecases/{{dashCase name}}';
import { makePrismaUpdate{{name}}InDatabaseRepository } from '@/main/factories/infra/database/orm/prisma/repositories/{{dashCase name}}';
import { makeList{{name}}sInDatabaseUsecase } from '@/main/factories/services/{{dashCase name}}';

export const makeUpdate{{name}}InDatabaseUsecase = (): Update{{name}}Usecase => {
  const list{{name}}sUsecase = makeList{{name}}sInDatabaseUsecase();

  const update{{name}}InDatabaseRepository =
    makePrismaUpdate{{name}}InDatabaseRepository();

  return new Update{{name}}InDatabaseService({
    list{{name}}sUsecase,
    update{{name}}InDatabaseRepository,
  });
};
