import { {{pascalCase name}}, {{pascalCase name}}DTO } from './{{dashCase name}}';

export class {{pascalCase name}}Transformer {
  static generateDTO({{camelCase}}: {{pascalCase name}}): {{pascalCase name}}DTO {
    return {
      id: {{camelCase name}}.id,
      enabled: {{camelCase name}}.enabled,
      name: {{camelCase name}}.name,
      createdAt: {{camelCase name}}.createdAt,
      updatedAt: {{camelCase name}}.updatedAt,
    };
  }
}
