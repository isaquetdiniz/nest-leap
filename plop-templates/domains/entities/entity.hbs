export interface {{pascalCase name}}DTO {
  id?: string;
  enabled?: boolean;
  name: string;
  createdAt?: Date;
  updatedAt?: Date;
}

export class {{pascalCase name}} {
  id?: string;
  name: string;
  enabled: boolean;
  createdAt?: Date;
  updatedAt?: Date;

  constructor({{camelCase name}}Params: {{pascalCase name}}DTO) {
    const { id, enabled, name, createdAt, updatedAt } =
      {{camelCase name}}Params;

    this.id = id;
    this.enabled = enabled ?? true;
    this.name = name;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;

    Object.freeze(this);
  }
}
