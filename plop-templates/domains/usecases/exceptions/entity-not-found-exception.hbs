import {
  DefaultException,
  ExceptionTypes,
} from '@/shared/helpers/error-helper';

import { {{pascalCase name}}DTO } from '@/domains/{{dashCase name}}';

export class {{pascalCase name}}NotFoundException extends DefaultException {
  constructor({{camelCase name}}: Partial<{{pascalCase name}}DTO>) {
    super({
      type: ExceptionTypes.USER,
      code: '{{constantCase name}}_NOT_FOUND',
      data: {{camelCase name}},
    });
  }
}
