import { {{name}} } from '@/domain/entities';

export interface Update{{name}}InDatabaseRepository {
  update{{name}}(
    {{lowerCase name}}Params: Update{{name}}InDatabaseRepository.Params
  ): Promise<Update{{name}}InDatabaseRepository.Result>;
}

export namespace Update{{name}}InDatabaseRepository {
  export type Params = {{name}};
  export type Result = void;
}
