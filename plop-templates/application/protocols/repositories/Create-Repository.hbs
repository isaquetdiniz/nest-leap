import { {{name}} } from '@/domain/entities';

export interface Create{{name}}InDatabaseRepository {
  create{{name}}(
    {{camelCase name}}Params: Create{{name}}InDatabaseRepository.Params
  ): Promise<Create{{name}}InDatabaseRepository.Result>;
}

export namespace Create{{name}}InDatabaseRepository {
  export type Params = {{name}};
  export type Result = void;
}
