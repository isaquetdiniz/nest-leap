import { List{{name}}sUsecase } from '@/domain/usecases/{{dashCase name}}';

export interface Count{{name}}sInDatabaseRepository {
  count{{name}}s(
    {{camelCase name}}Filters: Count{{name}}sInDatabaseRepository.Params
  ): Promise<Count{{name}}sInDatabaseRepository.Result>;
}

export namespace Count{{name}}sInDatabaseRepository {
  export type Params = Omit<
    List{{name}}sUsecase.Params,
    'take' | 'skip' | 'orderBy'
  >;
  export type Result = { total{{name}}s: number };
}
