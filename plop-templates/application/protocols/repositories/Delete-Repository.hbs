import { {{name}} } from '@/domain/entities';

export interface Delete{{name}}InDatabaseRepository {
  delete{{name}}(
    {{lowerCase name}}Params: Delete{{name}}InDatabaseRepository.Params
  ): Promise<Delete{{name}}InDatabaseRepository.Result>;
}

export namespace Delete{{name}}InDatabaseRepository {
  export type Params = {{name}};
  export type Result = void;
}
