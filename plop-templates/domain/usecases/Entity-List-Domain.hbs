import { {{name}} } from '@/domain/entities/{{name}}';

export interface List{{name}}sUsecase {
  list({{camelCase name}}Filters: List{{name}}sUsecase.Params): Promise<List{{name}}sUsecase.Result>;
}

export namespace List{{name}}sUsecase {
  export type Params = {
    userRequester?: {{name}};
    id?: string;
    name?: string;
    createdAt?: { inititalDate: string; finalDate: string };
    updatedAt?: { inititalDate: string; finalDate: string };
    take?: number;
    skip?: number;
    orderBy?: {
      property: string;
      mode: 'asc' | 'desc';
    };
  };
  export type Result = { {{camelCase name}}s: {{name}}[]; total{{name}}s: number };
}
