import { {{name}}, {{name}}Input } from '@/domain/entities/{{name}}';

export interface Create{{name}}Usecase {
  create(
    {{lowerCase name}}Params: Create{{name}}Usecase.Params
  ): Promise<Create{{name}}Usecase.Result>;
}

export namespace Create{{name}}Usecase {
  export type Params = Omit<{{name}}Input, 'id'> & { userRequester: {{name}} };
  export type Result = {{name}};
}
