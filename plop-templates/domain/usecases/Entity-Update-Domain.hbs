import { {{name}} } from '@/domain/entities/{{name}}';

export interface Update{{name}}Usecase {
  update(
    {{camelCase name}}Params: Update{{name}}Usecase.Params
  ): Promise<Update{{name}}Usecase.Result>;
}

export namespace Update{{name}}Usecase {
  export type Params = {
    userRequester: {{name}};
    id: string;
    isAdmin?: boolean;
    name?: string;
    email?: string;
    enabled?: boolean;
  };
  export type Result = {{name}} | Error;
}
